Class Solution:
def rotate(self, matrix: List[List[int]]) >
None:
1-0
rlen(matrix) -1
while lr:
matrix[1], matrix[r] matrix[r], matrix [1]
1-1
1
#transpose
for i in range(len(matrix)):
for j in range(1):
matrix[i][j], matrix[j][i]matrix[j] [i], matrix[i][j]
